plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.giorgia.giorgiashop"
    compileSdk = 35
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.giorgia.giorgiashop"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = 21
        targetSdk = 35
        versionCode = 101
        versionName = flutter.versionName

    }
    signingConfigs {
        debug {
            keyAlias 'GiorgiaShop'
            keyPassword '382020@Mido'
            storeFile file('giorgiashop-keystore.jks')
            storePassword '382020@Mido'
        }
        release {
            keyAlias 'GiorgiaShop'
            keyPassword '382020@Mido'
            storeFile file('giorgiashop-keystore.jks')
            storePassword '382020@Mido'
        }
    }
    buildTypes {
        debug {
            
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    buildscript {
        repositories {
            google()
            mavenCentral()
            jcenter()  // Optional, but can help in case of missing dependencies
        }
        dependencies {
            // Ensure you're using the correct Gradle plugin for Flutter
            classpath 'dev.flutter:flutter-gradle-plugin:1.0.0'  // Add this line to the dependencies
            classpath 'com.android.tools.build:gradle:7.4.0'  // Check for compatible Gradle plugin version
            classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.0'  // For Kotlin support
        }
    }
}

flutter {
    source = "../.."
}
dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"

}
dependencies {
    implementation 'com.google.android.gms:play-services-auth:20.7.0'
}